[project]
name = "RAG-based-Mutual-Fund-FAQ-Chatbot"
version = "1.0.0"
description = "A specialized RAG-based chatbot for Tata Mutual Fund FAQs."
requires-python = ">=3.11,<3.12"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
